<script>
    $(document).ready(function(){
        $("#preview_link_<%= item.id %>").colorbox();              
    });
</script>

<%= link_to(theme_image_tag("preview_video.png", :class => "thumbnail"), {:action => "show", :controller => "plugin_videos", :id => item.id, :video_id => record.id}, :id => "preview_link_#{item.id}", :title => record.title.blank? ? record.title : "") %>
