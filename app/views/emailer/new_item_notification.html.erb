<%= t("email.message.object_new_from_user", :object => @setting[:item_name], :name => @item.name, :title => @setting[:title], :from => @item.user.username, :url => @url) %>

<%#= url_for(:action => "view", :controller => "items", :id => @item, :only_path => false ) %>

