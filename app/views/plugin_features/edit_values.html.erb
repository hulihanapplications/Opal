<%= render :partial => "items/item_header", :locals => {:item => @item, :options => {:show_item_info => true, :show_item_title => true}} %>


<div style="height:10px"></div>

<div align=left class="box_style_2">
	<% features_plugin = Plugin.find_by_name("Feature") %>
	<% if features_plugin.is_enabled? || @logged_in_user.is_admin?  %>
		<a name="Features"></a>
		<table style="width:100%" cellpadding=0 cellspacing=0>
			<tr>
				<td align=left>
					<h3><%= features_plugin.human_name.pluralize %></h3>	
				</td>
				<td align=right>
					<%= link_to "Add/Edit Features", {:action => "index", :controller => "plugin_features"} if @logged_in_user.is_admin?  %>
				</td>
			</tr>
		</table>
	
		<div class="spacer"></div>
		<%= form_tag :action => 'update_values', :controller => "plugin_features", :id => @item.id  %>
			<%= render :partial => "plugin_features/form_for_item", :locals => {:item => @item} %>
			<div align=center><%= submit_tag "Save" %></div>
		</form>									
	<% end %>
	<div style="height:10px"></div>
</div>
<br>
<div align=center><img src="/themes/<%= @setting[:theme] %>/images/icons/arrow_left.png" class="icon" title="Back"> <%= link_to "Back", { :action => 'view', :controller => "items", :id => @item, :anchor => "#{@plugin.name}s"}  %></div>
	