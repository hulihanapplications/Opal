<h2><%= t("label.items_found", :count => @results.size, :items => Item.model_name.human(:count => @results.size)) %></h2>
<%= render :partial => "items/list_items", :locals => {:items => @results } %>

<%# For Debugging %>
<%#= "#{@options.inspect}<br><br>#{params.inspect} " %>

