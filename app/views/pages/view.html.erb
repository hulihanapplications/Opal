<% content_for :side do %>
	<% if params[:controller] == "blog" %>
		<%= render :partial => "blog/menu" %>
	<% else %>
		<%= render :partial => "menu_for_page", :locals => {:page => @page} if @page.display_children %>				
	<% end %>
<% end %>
	
<div class="page" id="page">
	<%= render @page %>
</div>
