<%= render :partial => "navigation" %>

<% content_for :side do %>
	<div align=center class="box_1">
		<h2><%= t("label.search_results_for", :query => h(@search_for)) %></h2>
	</div>
<% end %>

	<%= render :partial => "items/list_items", :locals => {:items => @results } %>
	<div class="pagination">
		<%= page_entries_info @results, :entry_name => Item.model_name.human %>	
		<%= will_paginate @results, :params => {:search_for => @search_for } %>		
	</div>
