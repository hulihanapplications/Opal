<%= t("email.message.item_new_from_user", :item => @setting[:item_name], :name => @item.name, :title => @setting[:title], :from => @item.user.username, :url => @url) %>

<%#= url_for(:action => "view", :controller => "items", :id => @item, :only_path => false ) %>

