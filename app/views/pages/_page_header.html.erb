<% 
	options ||= Hash.new
	options[:navlinks] = true if options[:navlinks].nil?
%>
<% if @logged_in_user.is_admin? %>
	<%= render :partial => "application/record_menu", :locals => {:record => page} %>
<% end %>
<%= nav_link_page page if options[:navlinks] && page %>